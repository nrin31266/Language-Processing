# FASTAPI
<!-- How to Run -->
1. Setup app_env and pip
bash
python3 -m venv app_env
source app_env/bin/activate

2. Install libraries and dependencies
bash
pip install -r requirements.txt

3. Run app
bash
uvicorn src.main:app --reload --port 8089


